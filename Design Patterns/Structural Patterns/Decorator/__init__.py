"""
Decorator Design Pattern :
    1. Decorator Method is a Structural Design Pattern which allows to dynamically attach new behaviours to objects
       without changing their implementation by placing these objects inside the wrapper objects that contains the
       behaviors.

    2. It is much easier to implement Decorator Method in python because of ist built-in feature . It is not equivalent
       to the Inheritance because the new feature is added only to that particular object , not to the entire subclass

    * Problem Without Using Decorator Method :
          
"""